<%
  # This partial dispatches the jobs necessary to generate the map and reduce functions for MapReduce queries.
%>

<%= render :partial => '/queries/builder/map_function' %>
<%= render :partial => '/queries/builder/reduce_function' %>

alert(fakeIt);

hDebugger.ace_query_map.getSession().setValue(map.toString());
hDebugger.ace_query_reduce.getSession().setValue(reduce.toString());