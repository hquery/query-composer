<%= render partial: 'shared/tab_menu', locals: { selected: {endpoints: true}}%>
 <div> 
  <%= render :partial=> "queries/sidebar" %>
</div>
<div id="mainPanel" >
  <h1>Gateway Status</h1>
    <div class="top_border form_top_border">
	  <div class="top_border_lower form_border_lower">
      </div>
    </div>
  <%= render :partial => 'endpoint_statuses' %>

  <%= link_to(new_endpoint_path, class: "btn primary m") do %><span>Add Gateway</span><% end if current_user.admin? %>
  <%= link_to({ :controller => 'endpoints', :action => 'refresh_endpoint_statuses' }, :class => "btn second", :remote => true) do %><span>Refresh Endpoints</span><% end %>
    <div class="bottom_border_lower relative">
      <div class="bottom_border">
      </div>
    </div>
</div>
