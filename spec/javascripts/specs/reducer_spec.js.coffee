# The tests here check that the reducer correctly shapes Value objects that are being aggregated.
# We perform the actual tests for correctness on the Ruby side.
describe "Reducer aggregation functions", ->
  it "should construct a Value object", ->
    
  it "should run a sum operation", ->
  
  it "should run a frequency operation", ->
    
  it "should run a mean operation", ->
    
  it "should run a median operation", ->
    
  it "should run a mode operation", ->