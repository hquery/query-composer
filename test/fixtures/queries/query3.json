{
  "expected_count" : null,
  "description" : "Count of patients by age range",
  "endpoints" : [ 
    { 
      "name" : "Endpoint",
      "next_poll" : null,
      "result" : "{fake_result : blah}",
      "result_url" : null,
      "status" : "Not Complete",
      "submit_url" : "http://127.0.0.1:3001/queues" 
    }
  ],
  "aggregate_result" : "{fake_aggregate : foo}",
  "filter" : "",
  "map" : "function(patient) {\r\n  emit(null, {\"count\":1});\r\n}",
  "reduce" : "function(key, values) {\r\n  var total = {\"count\": 0};\r\n  var length = values ? values.length : 0;\r\n  for (var i=0;i<length;i++) {\r\n    total.count+=values[i].count;\r\n  }\r\n  return total;\r\n}",
  "title" : "Patients by Age" 
}